apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gluster-registry-claim
spec:
  accessModes:
   - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      gluster: registry-volume
